import React from 'react'

const Home = () => {
  return (
    <div>
        <h1>HTML Introduction</h1>
        <ul>
            <li>
                React is a JavaScript library for building user interface (UIs) on the web.</li> 
                <li>React is a declarative, component based library that allows developers to build resuable UI components and it follows the Virtual DOM (Document Object Model) approach, which optimizes rendering performance by minimizing DOM updates. </li>
                <li>React is fast and works well with other tools and libraries.
            </li>
        </ul>
    </div>
  )
}

export default Home
